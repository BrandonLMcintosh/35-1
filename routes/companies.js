const express = require("express");
const db = require("../db");
const router = new express.Router();

router.get("", async () => {});

router.post("", async () => {});

router.get("/:code", async () => {});

router.put("/:code", async () => {});

router.delete("/:code", async () => {});

module.exports = router;
